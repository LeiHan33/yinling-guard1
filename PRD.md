# 银龄守护 · 产品需求文档（PRD）

| 属性 | 内容 |
|------|------|
| 产品名称 | 银龄守护 |
| 版本 | v1.0（第一版） |
| 文档状态 | 已确认，作为开发依据 |
| 目标平台 | Android |
| 目标应用 | 抖音（第一版仅支持抖音） |
| 最后更新 | 2026-08-11 |

---

## 1. 产品目标和使用场景

### 1.1 产品目标

**一句话定位：** 帮子女守护老人，在刷抖音时自动识别并跳过「洗脑、煽动、谣言类」短视频。

**核心目标：**

- 减少老人接触养生骗局、政治煽动、情绪操控、标题党谣言等有害内容
- 降低老人观念被误导的风险（核心痛点：相信谣言、伪科学）
- 让子女能够配置和维护屏蔽规则，无需时刻盯守
- 第一版以「可用、可验证」为优先，零成本、本地运行、无需联网

**价值主张：**

| 用户 | 价值 |
|------|------|
| 老人 | 刷视频更安心，少被误导；操作简单，愿意配合 |
| 子女 | 可配置屏蔽词和黑名单，查看拦截记录，持续维护防护规则 |

### 1.2 目标用户

| 角色 | 描述 | 第一版职责 |
|------|------|------------|
| 老人 | 65 岁左右，每天刷抖音 1–2 小时，安卓手机，愿意配合安装守护工具 | 日常使用，查看守护状态 |
| 子女 | 关心老人上网安全，发现新谣言后希望及时加入屏蔽 | 当面或通过密码进入管理页，维护词库和黑名单 |

### 1.3 使用场景

#### 场景 A：首次安装与配置

```
子女帮老人安装 App
  → 完成 4 步引导（欢迎、功能说明、开启无障碍权限、设置完成）
  → 确认守护已开启
  → 子女输入密码进入「子女管理」，检查/补充屏蔽词
```

#### 场景 B：日常刷抖音（核心场景）

```
老人打开抖音刷视频
  → 银龄守护在后台读取当前视频标题/作者信息
  → 命中屏蔽词或黑名单 → 自动上滑跳过 + 可选 Toast「已跳过可疑内容」
  → 记录写入本地拦截日志
  → 老人可在「首页」看到今日拦截数
```

#### 场景 C：子女维护词库

```
子女发现新的谣言话术
  → 打开银龄守护 → 设置 → 子女管理（输入密码）
  → 添加屏蔽词，选择分类
  → 即刻生效，无需重启
```

#### 场景 D：查看拦截效果

```
子女或老人打开「拦截记录」Tab
  → 按今天/昨天/全部筛选
  → 查看被跳过的视频标题、类型、命中关键词
  → 评估守护效果，决定是否补充词库
```

### 1.4 内容过滤范围（第一版）

| 分类 | 说明 | 示例 |
|------|------|------|
| 养生骗局 | 伪科学、夸大疗效 | 「治百病」「包治」「祖传秘方」 |
| 谣言 | 不实信息、伪内部消息 | 「内部消息」「央视不敢播」 |
| 煽动 | 极端政治、民族对立 | 「崩溃」「阴谋」「亡我中华」 |
| 标题党 | 制造焦虑、诱导转发 | 「震惊」「速看」「不转不是中国人」 |

**过滤策略：** 严格模式 — 宁可多拦，不可漏过有害内容。

---

## 2. 整体页面框架和导航结构

### 2.1 信息架构

```
银龄守护 v1.0
│
├── 引导流程（仅首次安装，4 页，线性）
│   ├── P01 欢迎页
│   ├── P02 功能说明
│   ├── P03 开启权限
│   └── P04 设置完成
│
├── 主界面（底部 3 Tab，日常使用）
│   ├── Tab 1 首页
│   ├── Tab 2 拦截记录
│   └── Tab 3 设置
│
├── 子女管理（密码保护，从设置进入）
│   ├── P05 管理主页
│   ├── P06 屏蔽词管理
│   ├── P07 添加屏蔽词
│   └── P08 账号黑名单
│
└── 运行时浮层（抖音前台运行时）
    └── 跳过提示 Toast
```

**页面总数：** 12 个页面/状态（8 个独立页面 + 3 个 Tab + 1 个浮层）

### 2.2 导航规则

| 入口 | 目标 | 返回方式 |
|------|------|----------|
| 首次启动 | 引导流程 P01→P04 | 完成后进入首页 Tab |
| 底部 Tab | 首页 / 记录 / 设置 | Tab 间切换，无返回栈 |
| 设置 → 子女管理 | 密码验证 → P05 | 左上角返回 → 设置 Tab |
| P05 → 屏蔽词管理 | P06 | 返回 → P05 |
| P06 → 添加屏蔽词 | P07 | 返回 → P06 |
| P05 → 账号黑名单 | P08 | 返回 → P05 |
| 首页 → 查看全部 | 拦截记录 Tab | Tab 切换 |

### 2.3 技术实现路径（第一版）

- **核心机制：** Android 无障碍服务（AccessibilityService）
- **监听对象：** 抖音 App 前台界面
- **识别方式：** 读取屏幕文字（标题、作者昵称）+ 关键词/黑名单匹配
- **响应动作：** 模拟上滑手势跳过当前视频
- **数据存储：** 本地 SQLite 或 JSON 文件，无云端依赖

---

## 3. 首页的内容结构

### 3.1 页面布局

```
┌─────────────────────────────┐
│  下午好，守护中               │  ← 问候语 + 状态提示
│  银龄守护                    │  ← 页面标题
├─────────────────────────────┤
│  ┌───────────────────────┐  │
│  │ ● 正在保护      [抖音]  │  │  ← 守护状态卡片
│  └───────────────────────┘  │
├─────────────────────────────┤
│  今日拦截 12  │  累计守护 3天 │  ← 统计数字区（两列）
├─────────────────────────────┤
│  最近拦截          查看全部 › │  ← 区块标题 + 跳转链接
│  ┌───────────────────────┐  │
│  │ 震惊！某国即将崩溃 [崩溃]│  │  ← 拦截预览条目 1
│  │ 医院不想让你知道的秘方[秘方]│  │  ← 拦截预览条目 2
│  └───────────────────────┘  │
└─────────────────────────────┘
  [首页]    [记录]    [设置]      ← 底部 Tab 栏
```

### 3.2 内容区块说明

| 区块 | 字段 | 数据来源 | 刷新时机 |
|------|------|----------|----------|
| 问候语 | 「上午好/下午好/晚上好，守护中/已关闭」 | 系统时间 + 守护开关状态 | 进入页面时 |
| 守护状态卡片 | 状态（正在保护/已关闭）、平台标签（抖音） | 本地守护服务状态 | 实时 |
| 今日拦截 | 整数，今日 0 点至今的拦截次数 | 本地拦截日志聚合 | 进入页面时 + 拦截发生后 |
| 累计守护 | 「N 天」，自首次开启守护以来的天数 | 本地配置：首次开启日期 | 进入页面时 |
| 最近拦截 | 最多 3 条，标题（截断 20 字）+ 命中词标签 | 本地拦截日志，按时间倒序 | 进入页面时 |
| 查看全部 | 文字链接 | — | 点击跳转拦截记录 Tab |

### 3.3 状态变体

| 状态 | 守护卡片 | 统计区 | 最近拦截 |
|------|----------|--------|----------|
| 正常守护中 | 绿点 +「正在保护」 | 正常显示数字 | 有数据则显示，无则「暂无拦截」 |
| 守护已关闭 | 灰点 +「已关闭」 | 数字冻结（显示关闭前的值或 0） | 不更新 |
| 无障碍权限丢失 | 红点 +「权限异常，点击修复」 | 同上 | 同上，卡片可点击跳转 P03 |

---

## 4. 每个模块的用途

### 4.1 引导流程模块

**用途：** 首次安装时，引导老人和子女完成权限配置，建立对产品的基本信任。

**包含页面：** P01 欢迎页、P02 功能说明、P03 开启权限、P04 设置完成

**为何必要：** 无障碍权限是核心能力，必须用户手动授权；引导流程降低老人/子女的配置门槛。

---

### 4.2 首页模块（Tab 1）

**用途：** 日常状态的「仪表盘」，让老人和子女一眼确认守护是否在运行、今天拦了多少。

**核心价值：** 建立信任感（「有人在保护我」）和效果可见性（「确实拦住了坏内容」）。

---

### 4.3 拦截记录模块（Tab 2）

**用途：** 完整展示历史拦截明细，供回顾、评估效果、决定是否补充词库。

**核心价值：** 可追溯、可验证，是产品效果的直接证据。

---

### 4.4 设置模块（Tab 3）

**用途：** 提供守护开关、基础配置项，以及进入子女管理的入口。

**核心价值：** 老人有基本控制权；子女有管理入口。

---

### 4.5 子女管理模块

**用途：** 密码保护的管理后台，供子女维护屏蔽词、账号黑名单、白名单，查看统计。

**核心价值：** 词库可持续更新，适应不断出现的新谣言话术。

---

### 4.6 守护引擎模块（后台服务，无独立页面）

**用途：** 核心过滤逻辑 — 监听抖音界面、匹配规则、执行跳过、写入日志。

**核心价值：** 产品的技术核心，无此模块则产品无功能。

---

### 4.7 运行时浮层模块

**用途：** 在抖音内跳过视频时，短暂提示用户「已跳过可疑内容」。

**核心价值：** 让老人知道「有东西被拦了」，增强产品存在感；可在设置中关闭。

---

## 5. 每个模块第一版最必要的数据和操作

### 5.1 引导流程

| 数据 | 说明 |
|------|------|
| `onboarding_completed` | 布尔值，是否已完成引导 |
| `first_guard_date` | 首次开启守护的日期（用于「累计守护 N 天」） |

| 操作 | 说明 |
|------|------|
| 点击「开始使用」 | P01 → P02 |
| 点击「下一步」 | P02 → P03 |
| 点击「去开启权限」 | 跳转系统无障碍设置页 |
| 权限开启后返回 | 检测权限状态 → P04 |
| 点击「进入首页」 | 写入 `onboarding_completed=true` → 首页 Tab |

---

### 5.2 首页

| 数据 | 说明 |
|------|------|
| `guard_enabled` | 守护开关状态 |
| `accessibility_granted` | 无障碍权限是否有效 |
| `today_block_count` | 今日拦截次数（聚合） |
| `total_guard_days` | 累计守护天数（计算值） |
| `recent_blocks[]` | 最近 3 条拦截记录（id, title, keyword, time） |

| 操作 | 说明 |
|------|------|
| 进入页面 | 刷新上述数据 |
| 点击「查看全部」 | 跳转拦截记录 Tab |
| 点击权限异常卡片 | 跳转 P03 开启权限 |

---

### 5.3 拦截记录

| 数据 | 说明 |
|------|------|
| `block_logs[]` | 拦截日志列表，每条含：id, timestamp, title, keyword, category, author |

| 操作 | 说明 |
|------|------|
| 切换筛选（今天/昨天/全部） | 按时间范围过滤 `block_logs` |
| 滚动列表 | 分页或全量加载（第一版全量即可，预估每日 < 100 条） |

**单条日志数据结构：**

```json
{
  "id": "uuid",
  "timestamp": "2026-08-11T14:32:00",
  "title": "不转不是中国人！内部消息...",
  "keyword": "内部消息",
  "category": "谣言",
  "author": "某某说天下"
}
```

---

### 5.4 设置

| 数据 | 说明 |
|------|------|
| `guard_enabled` | 守护开关 |
| `toast_enabled` | 跳过时是否显示 Toast |
| `filter_mode` | 固定为「严格」（第一版只读展示） |
| `target_app` | 固定为「抖音」（第一版只读展示） |
| `family_password_hash` | 子女管理密码（哈希存储） |
| `app_version` | 当前 App 版本号 |

| 操作 | 说明 |
|------|------|
| 切换守护开关 | 关：需输入子女密码；开：直接开启 |
| 切换跳过时提示 | 直接切换 `toast_enabled` |
| 点击「子女管理」 | 弹出密码输入 → 验证通过 → P05 |
| 点击「使用帮助」 | 打开帮助页（第一版可为静态文本页） |
| 首次进入子女管理 | 若未设密码，引导设置密码 |

---

### 5.5 子女管理

#### P05 管理主页

| 数据 | 说明 |
|------|------|
| `keyword_count` | 屏蔽词总数 |
| `blacklist_count` | 黑名单账号数 |
| `whitelist_count` | 白名单条目数 |

| 操作 | 说明 |
|------|------|
| 点击「屏蔽词管理」 | → P06 |
| 点击「账号黑名单」 | → P08 |
| 点击「白名单」 | → 白名单页（第一版可与黑名单合并为 P08 的 Tab） |
| 点击「拦截统计」 | → 统计页（第一版可简化为 P05 内嵌数字） |
| 点击「导出词库」 | 导出 JSON 文件到 Downloads |

#### P06 屏蔽词管理

| 数据 | 说明 |
|------|------|
| `keywords[]` | 屏蔽词列表，每条含：id, word, category, block_count |

| 操作 | 说明 |
|------|------|
| 分类筛选 | 按 category 过滤 |
| 删除词条 | 从列表和词库文件移除 |
| 点击「+ 添加屏蔽词」 | → P07 |

#### P07 添加屏蔽词

| 数据 | 说明 |
|------|------|
| 输入：word | 关键词文本，1–20 字 |
| 选择：category | 养生骗局 / 谣言 / 煽动 / 标题党 |

| 操作 | 说明 |
|------|------|
| 输入关键词 + 选择分类 | — |
| 点击「确认添加」 | 写入 `keywords[]`，即刻生效，返回 P06 |

#### P08 账号黑名单

| 数据 | 说明 |
|------|------|
| `blacklist[]` | 黑名单列表，每条含：id, author_name, added_at |

| 操作 | 说明 |
|------|------|
| 点击「+ 添加账号」 | 弹出输入框，输入作者昵称 |
| 删除账号 | 从列表移除 |

---

### 5.6 守护引擎（后台）

| 数据 | 说明 |
|------|------|
| `keywords[]` | 运行时加载的完整词库 |
| `blacklist[]` | 运行时加载的黑名单 |
| `whitelist[]` | 白名单（命中则不过滤） |

| 操作 | 说明 |
|------|------|
| 检测抖音前台 | AccessibilityEvent 触发 |
| 读取屏幕文字 | 提取标题、作者 |
| 匹配规则 | 标题含关键词 OR 作者在黑名单；白名单优先 |
| 命中 | 模拟上滑 + 写 block_log + 可选 Toast |
| 词库变更 | 监听本地文件变化，热加载 |

---

### 5.7 运行时浮层

| 数据 | 说明 |
|------|------|
| `toast_enabled` | 是否显示 |

| 操作 | 说明 |
|------|------|
| 拦截成功后 | 若 `toast_enabled`，显示 Toast 2 秒后消失 |

---

## 6. 首页和各模块之间的关系

### 6.1 数据流关系

```
                    ┌─────────────┐
                    │  守护引擎    │
                    │ （后台服务）  │
                    └──────┬──────┘
                           │ 写入
                           ▼
                    ┌─────────────┐
                    │ block_logs  │ ← 本地拦截日志
                    │ keywords    │ ← 本地词库
                    │ blacklist   │ ← 本地黑名单
                    └──────┬──────┘
                           │ 读取
          ┌────────────────┼────────────────┐
          ▼                ▼                ▼
    ┌──────────┐    ┌──────────┐    ┌──────────┐
    │   首页    │    │ 拦截记录  │    │ 子女管理  │
    │ 聚合统计  │    │ 完整列表  │    │ 词库 CRUD │
    └──────────┘    └──────────┘    └──────────┘
          ▲                                  │
          │                                  │ 修改词库
          └──────────── 词库变更后热加载 ◄───┘
```

### 6.2 页面跳转关系

```
引导流程 ──完成──► 首页 Tab ◄──Tab切换──► 拦截记录 Tab ◄──Tab切换──► 设置 Tab
                      │                                              │
                      │ 查看全部                                      │ 子女管理（密码）
                      └──────────────────────────────────► 拦截记录 Tab │
                                                                     ▼
                                                               子女管理主页
                                                                     │
                                              ┌──────────────────────┼──────────────────────┐
                                              ▼                      ▼                      ▼
                                        屏蔽词管理              账号黑名单              导出词库
                                              │
                                              ▼
                                        添加屏蔽词
```

### 6.3 状态依赖关系

| 模块 | 依赖 | 说明 |
|------|------|------|
| 守护引擎 | 无障碍权限 | 无权限则不运行，首页显示「权限异常」 |
| 守护引擎 | `guard_enabled=true` | 关闭时不拦截，但仍可记录（第一版：关闭即不拦截） |
| 守护引擎 | `keywords[]` 非空 | 词库为空时不过滤（应内置默认词库） |
| 首页统计 | `block_logs` | 无拦截记录时显示 0 和「暂无拦截」 |
| 子女管理 | 子女密码 | 未设密码时首次进入引导设置 |

---

## 7. 本地数据文件、备份和恢复要求

### 7.1 本地数据文件清单

所有数据存储在 App 私有目录：`/data/data/com.yinling.guard/files/`

| 文件名 | 格式 | 内容 | 读写方 |
|--------|------|------|--------|
| `config.json` | JSON | 应用配置（见 7.2） | 设置模块、引导流程 |
| `keywords.json` | JSON | 屏蔽词库（见 7.3） | 子女管理、守护引擎 |
| `blacklist.json` | JSON | 账号黑名单（见 7.4） | 子女管理、守护引擎 |
| `whitelist.json` | JSON | 白名单（见 7.5） | 子女管理、守护引擎 |
| `block_logs.json` | JSON | 拦截日志（见 7.6） | 守护引擎写入，首页/记录读取 |
| `keywords_default.json` | JSON | 内置默认词库（只读，随 APK 打包） | 首次安装复制到 keywords.json |

### 7.2 config.json 结构

```json
{
  "onboarding_completed": true,
  "guard_enabled": true,
  "toast_enabled": true,
  "filter_mode": "strict",
  "target_app": "douyin",
  "first_guard_date": "2026-08-11",
  "family_password_hash": "sha256:...",
  "app_version": "1.0.0"
}
```

### 7.3 keywords.json 结构

```json
{
  "version": 1,
  "updated_at": "2026-08-11T20:00:00",
  "keywords": [
    {
      "id": "kw_001",
      "word": "治百病",
      "category": "health_scam",
      "source": "builtin",
      "block_count": 0,
      "created_at": "2026-08-11T10:00:00"
    }
  ]
}
```

**category 枚举值：** `health_scam`（养生骗局）、`rumor`（谣言）、`incitement`（煽动）、`clickbait`（标题党）

### 7.4 blacklist.json 结构

```json
{
  "version": 1,
  "updated_at": "2026-08-11T20:00:00",
  "accounts": [
    {
      "id": "bl_001",
      "author_name": "某某养生堂",
      "added_at": "2026-08-11T15:00:00"
    }
  ]
}
```

### 7.5 whitelist.json 结构

```json
{
  "version": 1,
  "updated_at": "2026-08-11T20:00:00",
  "entries": [
    {
      "id": "wl_001",
      "type": "keyword",
      "value": "官方发布",
      "added_at": "2026-08-11T16:00:00"
    }
  ]
}
```

**type 枚举值：** `keyword`（关键词白名单）、`author`（作者白名单）

### 7.6 block_logs.json 结构

```json
{
  "version": 1,
  "logs": [
    {
      "id": "log_001",
      "timestamp": "2026-08-11T14:32:00",
      "title": "不转不是中国人！内部消息...",
      "keyword": "内部消息",
      "category": "rumor",
      "author": "某某说天下"
    }
  ]
}
```

**日志保留策略（第一版）：** 保留最近 30 天或最多 1000 条，超出自动清理最旧记录。

### 7.7 内置默认词库

- 文件：`keywords_default.json`（打包在 APK assets 目录）
- 数量：约 140 个词，覆盖 4 个分类
- 首次安装：若 `keywords.json` 不存在，从 `keywords_default.json` 复制

### 7.8 备份要求

| 项目 | 要求 |
|------|------|
| 导出格式 | JSON 文件 |
| 导出内容 | `keywords.json` + `blacklist.json` + `whitelist.json`（不含 block_logs） |
| 导出位置 | 手机 Downloads 目录，文件名 `yinling_guard_backup_YYYYMMDD.json` |
| 触发方式 | 子女管理 → 导出词库 |
| 导出结构 | 见 7.9 |

### 7.9 备份文件结构

```json
{
  "backup_version": 1,
  "exported_at": "2026-08-11T20:00:00",
  "app_version": "1.0.0",
  "keywords": [ ... ],
  "blacklist": [ ... ],
  "whitelist": [ ... ]
}
```

### 7.10 恢复要求

| 项目 | 要求 |
|------|------|
| 导入方式 | 子女管理 → 导入词库（第一版可选，若来不及做则仅支持导出） |
| 导入逻辑 | 读取备份 JSON → 合并到现有词库（同 id 跳过，新 id 追加）→ 写入本地文件 → 热加载 |
| 冲突处理 | 同 word 不同 category：保留已有，跳过新增 |
| 失败处理 | 格式错误时提示「备份文件无效」，不修改现有数据 |

### 7.11 数据安全

- 所有数据仅存本地，不上传云端
- 子女密码以 SHA-256 哈希存储，不存明文
- 拦截日志不含视频内容，仅标题和作者昵称

---

## 8. 第一版必须完成的功能

### 8.1 功能清单

| 编号 | 功能 | 模块 | 优先级 |
|------|------|------|--------|
| F01 | 首次引导流程（4 页） | 引导流程 | P0 |
| F02 | 无障碍权限检测与引导 | 引导流程 | P0 |
| F03 | 守护引擎：监听抖音、读取标题/作者 | 守护引擎 | P0 |
| F04 | 守护引擎：关键词匹配 | 守护引擎 | P0 |
| F05 | 守护引擎：黑名单匹配 | 守护引擎 | P0 |
| F06 | 守护引擎：自动上滑跳过 | 守护引擎 | P0 |
| F07 | 守护引擎：写入拦截日志 | 守护引擎 | P0 |
| F08 | 内置默认词库（约 140 词） | 数据 | P0 |
| F09 | 首页：守护状态展示 | 首页 | P0 |
| F10 | 首页：今日拦截数、累计守护天数 | 首页 | P0 |
| F11 | 首页：最近拦截预览（3 条） | 首页 | P1 |
| F12 | 拦截记录：按时间筛选、列表展示 | 拦截记录 | P1 |
| F13 | 设置：守护开关（关闭需密码） | 设置 | P0 |
| F14 | 设置：跳过时提示开关 | 设置 | P1 |
| F15 | 设置：子女管理入口 + 密码验证 | 设置 | P0 |
| F16 | 子女管理：屏蔽词列表、分类筛选 | 子女管理 | P1 |
| F17 | 子女管理：添加/删除屏蔽词 | 子女管理 | P1 |
| F18 | 子女管理：账号黑名单增删 | 子女管理 | P1 |
| F19 | 子女管理：导出词库 | 子女管理 | P2 |
| F20 | 运行时 Toast：「已跳过可疑内容」 | 浮层 | P1 |
| F21 | 词库热加载（修改后无需重启） | 守护引擎 | P1 |
| F22 | 权限丢失时首页提示 | 首页 | P1 |

### 8.2 开发顺序建议

```
Phase 1（核心可用）
  F02 → F03 → F04 → F06 → F07 → F08 → F09 → F13

Phase 2（完整体验）
  F01 → F05 → F10 → F11 → F12 → F14 → F15 → F16 → F17 → F20 → F21 → F22

Phase 3（管理完善）
  F18 → F19
```

---

## 9. 第一版暂时不做的功能

| 编号 | 功能 | 原因 | 计划版本 |
|------|------|------|----------|
| NF01 | 远程管理（微信小程序/网页） | 需要后端，超出零预算 | v2 |
| NF02 | 远程同步词库 | 依赖 NF01 | v2 |
| NF03 | 支持快手 | 先验证抖音方案 | v2 |
| NF04 | 支持微信视频号 | 先验证抖音方案 | v2 |
| NF05 | AI 智能识别 | 成本高，关键词 MVP 优先 | v3 |
| NF06 | iOS 版本 | 系统限制大 | 待定 |
| NF07 | 用户登录/账号体系 | 第一版本地即可 | v2 |
| NF08 | 拦截统计图表 | 第一版数字足够 | v2 |
| NF09 | 误拦申诉/反馈 | 第一版可通过白名单解决 | v2 |
| NF10 | 导入词库 | 若开发时间紧，仅做导出 | v1.1 |
| NF11 | 社区共享词库 | 需要后端 | v3 |
| NF12 | 与辟谣平台 API 对接 | 需要合作用 | v3 |
| NF13 | 视频内容 OCR/语音识别 | 技术复杂，第一版仅标题 | v3 |
| NF14 | 过滤模式切换（严格/平衡/温和） | 第一版固定严格 | v2 |
| NF15 | 多 App 同时守护 | 第一版仅抖音 | v2 |

---

## 10. 可以实际检查的验收标准

### 10.1 引导流程

| 编号 | 验收项 | 检查方法 | 通过标准 |
|------|--------|----------|----------|
| AC-01 | 首次安装进入引导 | 安装后首次打开 App | 显示 P01 欢迎页，不直接进入首页 |
| AC-02 | 引导流程完整 | 逐步点击至 P04 | 4 页均可正常显示和跳转 |
| AC-03 | 权限引导有效 | P03 点击「去开启权限」 | 跳转到系统无障碍设置页 |
| AC-04 | 权限开启后检测 | 开启权限后返回 App | P04 显示「守护已开启」 |
| AC-05 | 完成后不再显示引导 | 完成引导后杀进程重开 | 直接进入首页 Tab |

### 10.2 守护引擎

| 编号 | 验收项 | 检查方法 | 通过标准 |
|------|--------|----------|----------|
| AC-06 | 抖音前台监听 | 打开抖音，查看 App 日志 | AccessibilityService 正常触发 |
| AC-07 | 标题关键词拦截 | 在抖音刷含「治百病」标题的视频 | 自动上滑跳过，日志有记录 |
| AC-08 | 黑名单拦截 | 将某作者加入黑名单，刷该作者视频 | 无论标题内容，均跳过 |
| AC-09 | 白名单放行 | 将某词加入白名单，刷含该词视频 | 不跳过，正常播放 |
| AC-10 | 正常视频不拦截 | 刷不含关键词的正常视频 | 不跳过，正常播放 |
| AC-11 | 拦截响应时间 | 计时从识别到跳过 | ≤ 2 秒 |
| AC-12 | 守护关闭时不拦截 | 关闭守护开关，刷含关键词视频 | 不跳过 |
| AC-13 | 词库热加载 | 添加新词后不重启 App，刷含新词视频 | 新词生效，视频被跳过 |

### 10.3 首页

| 编号 | 验收项 | 检查方法 | 通过标准 |
|------|--------|----------|----------|
| AC-14 | 守护状态显示 | 开启/关闭守护 | 卡片显示「正在保护」/「已关闭」，颜色对应 |
| AC-15 | 今日拦截数准确 | 拦截 3 次后查看首页 | 显示「3」 |
| AC-16 | 累计守护天数 | 首次开启后次日查看 | 显示「2天」 |
| AC-17 | 最近拦截预览 | 拦截后查看首页 | 显示最新拦截标题和关键词 |
| AC-18 | 查看全部跳转 | 点击「查看全部」 | 跳转到拦截记录 Tab |
| AC-19 | 权限丢失提示 | 手动关闭无障碍权限 | 首页卡片显示「权限异常」 |

### 10.4 拦截记录

| 编号 | 验收项 | 检查方法 | 通过标准 |
|------|--------|----------|----------|
| AC-20 | 记录完整 | 拦截后查看记录 Tab | 含时间、标题、关键词、分类 |
| AC-21 | 时间筛选 | 切换今天/昨天/全部 | 列表正确过滤 |
| AC-22 | 空状态 | 无拦截时查看 | 显示「暂无拦截记录」 |

### 10.5 设置

| 编号 | 验收项 | 检查方法 | 通过标准 |
|------|--------|----------|----------|
| AC-23 | 守护开关 | 点击开关 | 开：直接生效；关：弹出密码输入 |
| AC-24 | 关闭需密码 | 不输入密码尝试关闭 | 无法关闭 |
| AC-25 | Toast 开关 | 切换后拦截视频 | 开：显示 Toast；关：不显示 |
| AC-26 | 子女管理密码 | 首次进入子女管理 | 引导设置密码 |
| AC-27 | 密码验证 | 输入错误/正确密码 | 错误：拒绝进入；正确：进入 P05 |

### 10.6 子女管理

| 编号 | 验收项 | 检查方法 | 通过标准 |
|------|--------|----------|----------|
| AC-28 | 屏蔽词列表 | 进入屏蔽词管理 | 显示内置 + 自定义词，含分类和拦截次数 |
| AC-29 | 分类筛选 | 点击「养生骗局」 | 仅显示该分类词 |
| AC-30 | 添加屏蔽词 | 添加「测试词」 | 列表出现，抖音刷含该词视频被跳过 |
| AC-31 | 删除屏蔽词 | 删除某词 | 列表移除，该词不再触发拦截 |
| AC-32 | 添加黑名单 | 添加某作者昵称 | 该作者视频均被跳过 |
| AC-33 | 删除黑名单 | 删除某作者 | 该作者视频恢复正常 |
| AC-34 | 导出词库 | 点击导出 | Downloads 目录出现 JSON 文件，内容正确 |

### 10.7 数据与备份

| 编号 | 验收项 | 检查方法 | 通过标准 |
|------|--------|----------|----------|
| AC-35 | 首次安装默认词库 | 安装后检查 keywords.json | 约 140 个词，4 个分类 |
| AC-36 | 数据持久化 | 添加词后杀进程重开 | 词库和日志均保留 |
| AC-37 | 日志自动清理 | 模拟 1001 条日志 | 最旧 1 条被清理，保留 1000 条 |
| AC-38 | 离线可用 | 关闭网络，完整走一遍流程 | 所有功能正常 |

### 10.8 整体验收

| 编号 | 验收项 | 检查方法 | 通过标准 |
|------|--------|----------|----------|
| AC-39 | 端到端场景 | 安装 → 引导 → 刷抖音 30 分钟 | 至少拦截 1 条含关键词视频，记录可查 |
| AC-40 | 老人可操作 | 请老人独立完成：查看首页、切换 Tab | 无需帮助即可完成 |
| AC-41 | 拦截准确率 | 准备 20 个测试视频（10 有害 + 10 正常） | 有害拦截率 ≥ 80%，正常误拦率 ≤ 10% |

---

## 附录 A：内置默认词库（概要）

| 分类 | 数量 | 示例 |
|------|------|------|
| 养生骗局 (health_scam) | ~40 | 治百病、包治、祖传秘方、医院不想让你知道、特效药 |
| 谣言 (rumor) | ~30 | 内部消息、央视不敢播、不转不是中国人、刚刚发生 |
| 煽动 (incitement) | ~30 | 崩溃、亡我中华、阴谋、颜色革命、必看 |
| 标题党 (clickbait) | ~40 | 震惊、速看、马上删、99%的人不知道、赶紧看 |

完整词库见 `assets/keywords_default.json`（开发时创建）。

---

## 附录 B：页面线框图

交互式线框图见：

`C:\Users\Amazon\.cursor\projects\d-test-Cursor\canvases\yinling-guard-wireframe.canvas.tsx`

在 Cursor 中打开该文件可查看全部页面的可视化线框。

---

## 附录 C：术语表

| 术语 | 说明 |
|------|------|
| 无障碍服务 | Android AccessibilityService，允许 App 读取其他 App 界面文字并模拟操作 |
| 守护引擎 | 后台运行的核心过滤服务 |
| 屏蔽词 | 命中即触发拦截的关键词 |
| 黑名单 | 按作者昵称屏蔽，该作者所有视频均跳过 |
| 白名单 | 命中则放行，优先级高于屏蔽词 |
| 热加载 | 修改词库后无需重启 App 即可生效 |
| 严格模式 | 过滤策略，宁可多拦不可漏过 |

---

## 修订记录

| 版本 | 日期 | 修订内容 |
|------|------|----------|
| 1.0 | 2026-08-11 | 初版，基于需求调研确认 |
